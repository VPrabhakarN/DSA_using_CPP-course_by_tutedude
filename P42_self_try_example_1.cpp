// C++ program for an example of array
#include<iostream>
using namespace std;

int main(){
    // defining an array 
    int a[];
    a[4] = {1,2,3,4};

    // accessing an array element
    cout << a[0] << endl;

    return 0;
}

// conclusion : -> So, In the case of array we cannot initialize size in array after it's definition.